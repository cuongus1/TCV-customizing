projection;
strict ( 2 );
use draft;

define behavior for ZC_RAP_EINV_PAYM alias EINVpaym
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}